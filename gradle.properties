# BUG: This setting causes kotlinx.serialization to fail on subsequent test runs
# First run usually passes, subsequent runs fail with SerializationException
kotlin.js.ir.output.granularity=whole-program

